global:
  root_path: /mnt/parscratch/users/acp21rjf/floras50/checkpoints_seq_scheduler_rb_6epoch/n_seq_sched_
  name: floras50tuning
  overlap_ratio: 0.875
  checkpoint: step_182892.pt
  description: "tuning model training on floras50 data"



models:

  - name: floras50tuning
    repeat: 1
    seq_len: 32768
    overlap_ratio: 0.875
    path: ${global.root_path}${.seq_len}_rp_${.repeat}/${global.checkpoint}
  
  - name: floras50tuning
    repeat: 2
    seq_len: 32768
    overlap_ratio: 0.875
    path: ${global.root_path}${.seq_len}_rp_${.repeat}/${global.checkpoint}

  - name: floras50tuning
    repeat: 3
    seq_len: 32768
    overlap_ratio: 0.875
    path: ${global.root_path}${.seq_len}_rp_${.repeat}/${global.checkpoint}



args:
  verbose: true
  model_class: SCConformerXL
  save_dataframe_path: './results/thesis/floras50_tuning.csv'
  evaluation_mode: windowed_attention
  max_sequence_length: 3600000
  tokenizer_path: "/users/acp21rjf/long-context-asr/lcasr/artifacts/floras50/tokenizer.model"

datasets:
  - name: earnings22_full
    splits: [test]
  - name: this_american_life
    splits: [test, dev]
  - name: tedlium
    splits: [test, dev]
    args:
      single_utterance: false
      concat: false
  - name: rev16
    splits: [test]
  - name: earnings22
    splits: [test, dev]